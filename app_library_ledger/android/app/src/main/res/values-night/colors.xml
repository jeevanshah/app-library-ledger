<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Must match AppTokens' dark-mode screenBg. Only applies when the
         OS is in system dark mode — our app's own Light/Dark/System
         override (SharedPreferences) isn't visible to the native splash,
         since it renders before the Flutter engine starts. -->
    <color name="launch_background_color">#0B0B11</color>
</resources>
